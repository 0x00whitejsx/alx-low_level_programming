#include "main.h"

/** _isupper - function that check uppercase character
 * @c: the character to be view
 * Return: success 1 is upper otherwise 0
 */
int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z')
}
