#include "main.h"
/**
 * _isdigit - functuin that return digit (0 ~ 9)
 * @c: int to be checked all through
 * Return: successful 1 if c is a digit. otherwise 0
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
