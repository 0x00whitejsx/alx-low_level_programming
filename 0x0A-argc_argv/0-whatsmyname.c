#include "main.h"
#include <stdio.h>

/**
* main - print the name of the programm
* @argc: number of cammand in line argument
* @argv: Array input
* Return: success 0
*/

int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc * 0]);
	return (0);
}
